# Example secrets file for sops-nix
#
# Setup:
# 1. Generate age key: mkdir -p ~/.config/sops/age && age-keygen -o ~/.config/sops/age/keys.txt
# 2. Get public key: age-keygen -y ~/.config/sops/age/keys.txt
# 3. Update .sops.yaml with your public key
# 4. Create encrypted file: sops secrets/default.yaml
# 5. Copy structure below and fill with your actual values
# 6. Stage for Nix flakes: git add -f secrets/default.yaml
#
# NOTE: This file should be encrypted with sops before committing!

# 1Password service account token (the ONLY secret stored in sops-nix)
# All other secrets (AI keys) are fetched from 1Password at runtime
op:
  service_account_token: "your-1password-service-account-token"

# =============================================================================
# Git config is set directly in home.nix (not secrets - visible in commits)
# =============================================================================
#
# dev-config.git = {
#   userName = "Your Name";
#   userEmail = "you@example.com";
#   signing.key = "ssh-ed25519 AAAAC3...";  # SSH public key
# };
#
# =============================================================================
# AI service keys are stored in 1Password (fetched at shell startup)
# =============================================================================
#
# 1Password Item: xsuolbdwx4vmcp3zysjczfatam (vault: Dev)
# Fields:
#   - ANTHROPIC_API_KEY
#   - OPENAI_API_KEY
#   - GOOGLE_AI_STUDIO_KEY
#   - LITELLM_KEY
#   - OPENROUTER_API_KEY

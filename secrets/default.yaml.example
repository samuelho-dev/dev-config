# Example secrets file for sops-nix
#
# Setup:
# 1. Generate age key: mkdir -p ~/.config/sops/age && age-keygen -o ~/.config/sops/age/keys.txt
# 2. Get public key: age-keygen -y ~/.config/sops/age/keys.txt
# 3. Update .sops.yaml with your public key
# 4. Create encrypted file: sops secrets/default.yaml
# 5. Copy structure below and fill with your actual values
# 6. Stage for Nix flakes: git add -f secrets/default.yaml
#
# NOTE: This file should be encrypted with sops before committing!

# Git configuration
git:
  userName: "Your Name"
  userEmail: "your@example.com"
  # SSH public key for signing (not private key!)
  # Get from: cat ~/.ssh/id_ed25519.pub
  signingKey: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... your@example.com"

# NOTE: AI service API keys are fetched directly from 1Password vault
# Vault UUID: xsuolbdwx4vmcp3zysjczfatam
# Access via: op item get xsuolbdwx4vmcp3zysjczfatam --fields label=field_name
# Example in shell hooks:
#   anthropic_key=$(op item get xsuolbdwx4vmcp3zysjczfatam --fields label=anthropic_key)
#   openai_key=$(op item get xsuolbdwx4vmcp3zysjczfatam --fields label=openai_key)
